services:
  - type: web
    name: flashbuzz
    runtime: static
    buildCommand: npm install && npx vite build
    staticPublishPath: ./dist/public
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
